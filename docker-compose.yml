services:
  frontend:
    build:
      context: ./zoo-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build:
      context: ./kpo-backend
    ports:
      - "8080:8080"
